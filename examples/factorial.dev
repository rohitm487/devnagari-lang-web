// Factorial calculation in Devanagari Programming Language
फंक्शन factorial(n) {
    अगर n <= 1 {
        वापस 1;
    }
    वापस n * factorial(n - 1);
}

// Calculate factorial of numbers 1 to 5
केलिए (वैरिएबल i = 1; i <= 5; i = i + 1) {
    वैरिएबल result = factorial(i);
    छाप i + "! = " + result;
} 